services:
  web:
    build: .
    volumes:
      - .:/rails
    ports:
      - "3000:3000"
    environment:
      - RAILS_ENV=development
    stdin_open: true
    tty: true
    command: ./bin/rails server -b 0.0.0.0
